#
# sqlite config file for local(8) aliases(5) lookups
#

# Path to sqlite db
dbpath = /path/to/web2postfix/databases/storage.sqlite

query = SELECT goto FROM mail_alias WHERE address='%s' AND active='T'

