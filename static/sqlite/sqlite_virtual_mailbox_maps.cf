#
# sqlite config file for virtual mailboxes(5) lookups
#

# Path to sqlite db
dbpath = /path/to/web2postfix/databases/storage.sqlite

query = SELECT maildir FROM mailbox WHERE mail_address='%s' AND active = 'T'

